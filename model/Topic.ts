export interface Topic {
    title: string
    description: string
    code: string
    flashcardsCount: number
    lastReviewedOn: string
    lastScore: number
    blog_url: string
}

export interface TopicSection {
    title: string
    code: string
}